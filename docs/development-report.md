# Data Coffee 首版本地开发交付

读者：发布者与产品验收者。日期：2026-09-05。

## 可以试用的内容

本地入口：http://localhost:8787 。独立开发路径：`/Users/zhaidewei/SHUJUQUN/data_coffee_terminal/app`。旧代码及数据库保留。浏览器已有一场明确标为“本地验收”的十月测试活动，不是真实征集。

- 邮箱登录、昵称隐私、创建草稿、预览发布、规则锁定。
- 本人报名退出、候补顺序递补、固定最低人数、独立条件补齐倒计时、自动取消及状态回执。
- 协办和现场负责人、分享、场地、物资、赞助意向申请与审核。
- 可视化决策流程、移动布局、后台状态刷新、介绍修订历史。
- DeepSeek接口与需本人点击的确认卡，后端再次检查权限、版本及规则；未接入时使用常规按钮。
- 持久邮件队列、限额、重试与身份验证防护。

## 已验证的结果

42项自动化测试通过，TypeScript及Workers构建通过。浏览器主路径“登录→草稿→发布→报名→退出→刷新”通过，并检查390px移动布局。独立代码评审发现的开始后紧急取消和介绍历史问题已修复并加入回归测试。

## 下一阶段验收

当前没有部署到workers.dev，没有修改DNS、真实发信或调用DeepSeek。云端D1仍为配置占位，接下来需完成真实绑定、邮件发信域验证、secrets注入以及远端Cron和CPU预算验证。邮件免费额度不保证700人同日都收到，通知可能排队；页面状态以数据库结果为准。

产品验收时需确认一处简化：首版正式报名在活动开始前可退出，没有另设发布者配置的退出锁定期限。这遵循后续“退出→递补→补齐”的简化，但与早期需求里的退出期限字段有差异。候补进行中仍可退出，发布者可紧急取消。

这一版本可用于本地产品验收，真实十月活动发布仍需云端接入及实际收件验证。
